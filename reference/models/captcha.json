{
  "title": "Captcha",
  "type": "object",
  "properties": {
    "captchaKey": {
      "type": "string",
      "description": "The captcha key.",
      "writeOnly": true
    }
  },
  "required": [
    "captchaKey"
  ],
  "description": "If this model is applied to a route, it will enforce that a user specifies a captcha key to secure the route."
}